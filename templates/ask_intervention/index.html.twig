{% extends 'base.html.twig' %}

{% block title %}Demande Intervention{% endblock %}

{% block body %}
    <div class="container">
        <h1>Demande d'intervention</h1>
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-success">Demander</button>
        {{ form_end(form) }}
    </div>
{% endblock %}
